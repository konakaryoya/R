services:
  r-dev-env:
    image: rocker/tidyverse:latest
    volumes:
      - ..:/home/rstudio/project:cached
    environment:
      - USER=rstudio
      - PASSWORD=password
    ports:
      - "8787:8787"
