services:
  r-dev-env:
    image: rocker/tidyverse:latest
    volumes:
      - ..:/home/rstudio/project:cached
    environment:
      - USER=rstudio
      - PASSWORD=password
    # AntigravityのManager Viewからポート8787でRStudioを並行利用することも可能
    ports:
      - "8787:8787"
